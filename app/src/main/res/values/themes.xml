<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.BiometricAppLock" parent="@android:style/Theme.DeviceDefault.NoActionBar" />

    <style name="Theme.BiometricAuth" parent="@android:style/Theme.Translucent.NoTitleBar">
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowAnimationStyle">@null</item>
    </style>

    <!-- compat fallback: solid window for OEMs where the translucent prompt self-cancels -->
    <style name="Theme.BiometricAuth.Opaque" parent="@android:style/Theme.DeviceDefault.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowAnimationStyle">@null</item>
        <!-- black splash with no launcher icon (api 31+); preview off for api 30 -->
        <item name="android:windowDisablePreview">true</item>
        <item name="android:windowSplashScreenBackground">@android:color/black</item>
        <item name="android:windowSplashScreenAnimatedIcon">@android:color/transparent</item>
    </style>
</resources>
